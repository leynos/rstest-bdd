[package]
name = "rstest-bdd-macros-fixtures"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
rstest-bdd = { version = "0.2.0-alpha1", path = "../../../rstest-bdd" }
rstest-bdd-macros = { version = "0.2.0-alpha1", path = "../.." }
